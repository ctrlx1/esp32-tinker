---
import { withBase } from "../lib/paths";

interface Props {
  href: string;
  label?: string;
}
const { href, label = "Download firmware.bin" } = Astro.props;
---
<a class="download" href={withBase(href)}>{label}</a>
